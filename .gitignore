# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local-only / personal artifacts (kept out of the public repo)
/reports/
/NQmain.jpg

# Ad-hoc manual query output (regenerated on demand by query_dates.py)
data/query_result.json

# NOTE: data/ is intentionally NOT ignored — the CSV/JSON outputs are the
# whole point of this repo and must be committed.
